query Country($code: ID!) {
    country(code: $code) {
        name
        phone
        continent {
            name
        }
        capital
        currency
        languages {
            name
        }
        emoji
    }
}